# Community Publishing Notes

There is no official niri "plugin registry" at the moment.
The common path is:

1. Publish as a normal GitHub repo.
2. Add it to the `awesome-niri` list via PR.

Suggested category: `Desktop Components` or `Widgets and Tools` (maintainer decision).

Include in README:
- clear alpha status
- compatibility notes
- install and rollback steps
